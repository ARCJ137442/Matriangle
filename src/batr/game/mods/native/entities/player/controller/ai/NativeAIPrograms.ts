import { AIActionGenerator } from "./AIControllerGenerator";

/**
 * 使用「行为生成器」的形式，用生成函数还原所有先前的「AI程序」
 */
export module NativeAIPrograms {
    export function* AIProgram_Dummy(): AIActionGenerator {

    }
}